# 卡片交互续聊约定 v1

本约定由交互事件生产插件和“引用续聊”插件共同遵守，**不是 AstrBot 原生标准**。接收端最低版本为 0.3.0，目前适配 AstrBot 4.28.0 飞书内置 Agent。双方使用以下名称和字段，不做别名推断。

## 事件结构

生产插件在事件进入 AstrBot 队列前设置：

```python
event.set_extra(
    "conversation_continuation_v1",
    {
        "version": 1,
        "source": "card_interaction",
        "event_id": interaction_id,
        "origin_message_id": original_question_message_id,
    },
)
```

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `version` | 整数 | 必须为 `1`，布尔值不接受 |
| `source` | 字符串 | 必须为 `card_interaction` |
| `event_id` | 字符串 | 本次按钮或表单操作的稳定唯一编号；同一回调重试保持不变，每次新操作不同 |
| `origin_message_id` | 字符串 | 原卡片所对应、已经登记的真实用户提问消息编号；不是卡片编号或本次操作编号 |

两个编号必须非空、不带首尾空白或控制字符，最多512字符。未知附加字段不参与路由；不支持的版本、来源或错误结构会被拒绝。缺少整个 extra 时按普通消息处理；存在但为 `None` 或字符串时属于错误结构。

## 生产端责任与信任范围

- 先验证平台回调真实性及操作人的实际权限，再构造事件。原提问编号应来自生产插件维护的可信卡片关联，不能直接信任可编辑表单值。
- 平台实例、机器人、聊天和实际操作人都来自已认证事件，不能从模型输出或表单文字中复制身份字段。群聊允许同群成员共享，私聊按实际操作人隔离。
- 每次操作创建独立的事件和 `message_obj`，不要复用仍在处理的事件对象。保留实际操作人的账号和昵称。
- `message_obj.message_id` 必须非空，可以是本次合成事件编号，也可以是原提问编号；**去重以 `event_id` 为准**。合成编号不得与其他话题的消息编号碰撞。
- 将按钮意图或表单内容作为普通用户输入，走标准的唤醒、权限和内置 Agent 处理链。事件必须有有效请求内容，并符合 AstrBot 的触发条件。
- 在入队前写入 extra。不要等内置 Agent 已开始执行后才补写，也不要在处理期间改写身份、编号、UMO 或 extra。不要复制本插件的内部 binding extra。
- 不通过 `provider_request` 接管模型请求，不跳过普通插件事件处理阶段，不在独立的后台任务中绕过处理链发送后续回复。

extra 是已安装插件之间的信任边界，不是面向聊天用户开放的路由接口。接收端不读取其他插件的代码或数据库，也无法替生产端验证平台回调签名。其他合法交互插件可按同一约定接入，无需安装指定的卡片插件。

## 路由、去重与冲突

仅在总开关、平台实例、群聊或私聊范围允许且不在排除列表时处理。

1. 有合法元数据时，只在可信会话范围的本地索引中查找 `origin_message_id`，且必须是登记过的原始用户输入。
2. 同时存在普通引用时，也要解析引用；二者必须指向同一话题，否则拒绝。多个引用编号不一致或引用无法验证也拒绝。
3. 没有续聊元数据时，普通引用恢复原话题，未引用请求新建话题。
4. 找不到关联、话题已删除或数据库归属不符时，停止请求并提示，不新建话题、不沿用最近对话。

交互去重使用独立的 `(scope, event_id)` 表，普通消息编号不会与其混为一谈。相同回调只处理一次；相同编号携带不同操作人或原提问则拒绝。群号、用户号、平台实例和机器人参与 scope，不进行全库消息编号搜索。

接收端在模型运行前同时提交交互记录和合成消息别名。模型失败或生成取消后仍保留去重记录，防止平台重投触发重复执行；用户主动重试应产生**新的操作编号**。等待锁期间取消、路由校验失败或事务失败不会占用该操作编号。

## 飞书回复链：合成编号不是回复目标

AstrBot 4.28.0 的原生 `LarkMessageEvent.send()` 与 `send_streaming()` 会用 `message_obj.message_id` 调用飞书回复接口。飞书无法将合成事件编号作为真实消息目标。

因此接收端在路由成功后，临时将本事件的 `message_obj.message_id` 设为已校验的 `origin_message_id`，保持至整条管道完成发送与历史写入，然后恢复原值。`event_id`、原合成消息编号和话题关联持久化保存，实际操作人和 UMO 不变。生产插件需要将这个临时字段视为**发送目标**，操作身份始终读取约定中的 `event_id`。

原生普通回复与流式卡片因此都回复真实原提问。后续引用时只需读取机器人回复，其 `parent_id` 会命中原提问索引，直接恢复话题；不会向飞书查询合成事件编号。重启后仍能恢复。索引中也保留合成消息别名，可终止已有别名父链的回溯。

**自定义发送器必须遵守同一规则**：回复真实原提问并保留消息父子关系。主动创建消息、只在原卡片上修改内容、脱离管道发送或忽略回复目标的发送器，不能仅凭这四个字段保证新消息可恢复；当前协议不提供发送回执登记接口。不要将合成编号冒充飞书消息编号，也不要擅自新增或改名字段来绕过这一限制。

路由失败时不向未校验的原消息或合成编号回复，而是在可信的当前群或操作人私聊中发送错误提示。即便提示发送失败，也保持事件停止。

## 管道顺序与并发

源码核对表明 EventBus 和 PipelineScheduler 传递同一个事件对象；extra 存于事件 `_extras`，标准管道不清空它。

但内置 Agent 的 `try_capture_follow_up()` 发生在 `on_waiting_llm_request` 之前，可能把同一用户的新操作合并到旧回答。因此接收端使用高优先级普通事件处理器，提前按物理会话取得现有话题锁并完成路由；后面的 waiting hook 识别已有绑定，不重复登记。等待期间和模型请求前再次校验元数据与身份。

同群操作串行，不同群或私聊独立；锁持续到完整管道结束。正常、异常或取消后都恢复原会话选择、原消息编号并释放锁。生产端不得绕过这个早期处理器，或在其运行之后才补写协议。

## 持久化与兼容性

插件索引由 schema 1 自动升级为 schema 2，保留旧话题和消息关联，新增交互去重表。更新前备份插件数据；旧版不认识 schema 2，回退代码时需要配套恢复旧版备份。无需修改 AstrBot 核心或其他插件数据库。

## 离线验证

单元测试覆盖交错话题、跨成员操作、私聊和跨群隔离、重复回调、缺失关联、禁用范围、冲突、异常、取消、迁移和重启。

`tests/integration_offline.py` 使用真实 AstrBot 4.28.0 事件、调度器、对话管理器及原生普通/流式发送方法，替换模型和飞书 SDK 的 I/O，禁止网络连接；校验实际构造的回复目标、extra 保留、重启后引用恢复，以及核心 follow-up 捕获前的排队。调度阶段使用测试夹具，不等同于生产完整管道或任意第三方卡片插件的端到端验收。
