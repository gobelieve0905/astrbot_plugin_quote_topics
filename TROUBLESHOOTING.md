# 常见问题

## 安装成功但没有效果

同时检查插件已加载、总开关、群聊或私聊开关、全部或指定列表、排除列表和平台实例范围。在目标会话发送 `/话题状态`。安装后的默认状态是关闭，不会自动改变所有群的聊天方式。

插件仍要求原有的 @、唤醒词和权限条件。它不会让普通群聊自动触发模型。

## 插件无法安装或加载

- 确认 AstrBot 版本是 4.28.0、Python 为3.12及以上，平台为飞书。
- 仓库下载或验证失败时，先检查服务器能否访问 GitHub；也可使用完整插件 ZIP 从文件安装。这类网络错误与模型或飞书权限不同。
- 如果日志提示不支持的索引版本，按 [升级说明](UPGRADING.md) 检查是否只回退了代码、没有恢复配套数据。不要直接删除数据库。
- 配置保存后仍未更新时，等当前请求结束再重新加载插件。

## 引用原始提问可以，引用机器人回复不行

读取机器人回复需要飞书应用具备读取对应消息的权限，而且回复必须保留与已登记提问的父子关系。检查飞书应用权限、权限变更是否生效及消息是否仍存在。

主动创建的新消息或某些自定义发送器没有回复关系，不能自动定位话题。可引用已登记的原始提问继续讨论。

## 引用了失败卡片，但模型不知道原问题

话题关联与问答历史是两件事。模型请求失败、空回答或任务取消时，AstrBot 可能没有保存本轮提问；引用仍可进入同一话题，但模型看不到缺失的内容。

开发版已增加兜底：请求结束后，如果核心历史没有变化，在释放话题锁前保存本轮文本提问和群聊发言人。已有正常回答、部分回答或检查点不被覆盖。此兜底不保存附件内容，也不能补回更新前已丢失的提问；旧话题请引用原提问并重新写出完整问题。模型服务失败本身仍需检查模型配置与连接。

## 旧消息无法恢复

安装前未登记的消息、已删除话题或遗失的插件索引都不能按消息内容猜测恢复。先确认备份包含插件索引和 AstrBot 对话数据。没有可用关联时，需不引用消息重新建立话题。

## 不引用“再详细一点”却新建了话题

这是启用后的预期规则。需要接着讨论时，引用对应话题中的提问或机器人回复。

## 自动标题没有生成

确认当前会话已启用引用续聊，并开启“自动总结话题标题”。话题需要有问答记录，已有编号话题在下一次续聊时才尝试，不扫描所有旧对话，也不处理其他来源的“无标题对话”。

等待后台任务完成后刷新对话列表。模型或备用模型不可用、超时、返回不合格标题，以及后台队列繁忙，都可能保留原名。可检查模型配置，或将标题等待时间在20–240秒范围内调整；整个任务最长300秒。

日志中的 `Quote topics title saved` 表示已写入；`title provider failed` 或 `title generation failed` 表示模型调用失败；`title skipped` 表示输出不符合标题要求。日志不会打印标题正文。

## 卡片按钮或表单没有进入原话题

本插件不提供按钮渲染或回调接收服务。生产交互事件的插件必须接入 [conversation_continuation_v1](CONTINUATION_PROTOCOL.md)，在入队前写入可信 extra，并使用原始用户提问作为真实回复目标。

每次新操作使用新 `event_id`，同一回调重试保留原编号。重复回调被停止是正常去重；模型失败或生成取消后，用户主动重试也应产生新操作编号。原提问编号不能拿来充当所有操作的去重编号。

元数据错误、关联缺失、身份不符或引用冲突会明确拒绝。接入方需查看协议与离线验证说明，不要从表单文字或模型输出中解析路由指令。

## 反馈问题

请提供 AstrBot、Python 和插件版本、平台类型、脱敏配置、复现步骤，以及对应时间点的简短错误信息。卡片问题还需提供交互插件名称和版本，说明它是否实现了约定。

不要提供代理订阅、API Key、飞书 App Secret、完整聊天记录或生产数据库。项目地址：[GitHub Issues](https://github.com/gobelieve0905/astrbot_plugin_quote_topics/issues)。
