# Unreleased

- 平台实例、群聊和私聊的指定/排除列表改为原生下拉多选，动态加载已有飞书实例和会话，并保留历史配置 ID。

# 更新记录

## 0.3.1

- 修复工具调用链超过 12 条记录时自动标题漏读最终回答的问题；过滤工具消息后再限制摘要条数，并兼容多次失败重试。
- 修复模型失败或请求取消且核心未保存历史时，后续引用只剩“再回答一次”的问题：在话题锁释放前保留本轮文本提问及群聊发言人，不覆盖核心已保存的历史。

- 安装包自动排除开发文档与协议说明，用户文档中的开发资料链接指向 GitHub。

- 建立 main/develop 分支规范、候选验收与正式标签，以及 AstrBot Cloud 手动送审流程。

## 0.3.0

- 补充社区用户安装、最小配置、升级回退和故障排查说明；市场简介明确支持范围与卡片接入条件。

- 新增 `conversation_continuation_v1` 交互续聊入口，按启用范围恢复原用户提问的话题。
- 使用独立交互编号去重，保存合成消息关联；校验实际操作人、会话范围与引用冲突。
- 交互事件在核心 follow-up 捕获之前进入会话锁，避免并入另一话题；异常和取消后恢复发送编号与会话选择。
- 原生普通回复和流式卡片使用真实原提问作为回复目标，支持重启后继续引用恢复，不查询合成消息编号。
- 索引升级至 schema 2；回退旧版需要配套恢复旧版数据备份。
- 新增通用事件约定与离线验证，不依赖卡片插件、不修改 AstrBot 核心。

## 0.2.4

- 整理社区发布说明，精简插件简介与提示语，集中维护开发资料。
- 安装包排除开发文档和测试文件，功能与配置保持兼容。

## 0.2.3

- 取消24字符硬截断，48字符以内的标题完整保存，避免中英文混排标题截断词语。
- 超长标题最多进行一次模型精简；仍不合格时保留原名，不截取半句话。

## 0.2.2

- 修复推理模型被20秒标题超时提前中断：单模型默认120秒、可配置20–240秒，整体上限300秒。
- 增加标题保存成功与无效输出的日志，不记录对话内容或模型凭据。

## 0.2.1

- 修复主模型不可用时自动标题失败：按当前会话内置 Agent 配置尝试备用模型，跳过重复与不存在的模型。
- 每个候选调用限时 20 秒，总任务仍限时 45 秒；全部失败保留原名，下次续聊重试。

## 0.2.0

- 新增“自动总结话题标题”开关，默认关闭。
- 问答完成后后台调用当前会话模型，将编号标题概括成简短标题；不修改聊天历史。
- 旧编号话题续聊时补生成；模型失败保留原标题，下次续聊重试。
- 限制后台任务数量，卸载时取消任务，并检查手动改名、删除和开关变更。

## 0.1.0

- 新建独立引用话题插件，默认关闭，群聊和私聊分别配置范围。
- SQLite 持久化消息关联；群内共享、私聊和平台实例隔离。
- 通过飞书消息父子关系恢复机器人回复，不依赖卡片插件或修改 AstrBot 核心。
- 完整请求生命周期内同群排队，完成后恢复原来的会话选择。
- 未知引用、缺失权限、话题删除、冲突和超时均明确停止恢复。
