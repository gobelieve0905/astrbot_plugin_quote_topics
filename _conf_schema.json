{
  "enabled": {
    "description": "总开关",
    "type": "bool",
    "default": false,
    "hint": "开启后：引用续聊，不引用新开。仅支持飞书和 AstrBot 4.28.0 内置 Agent。"
  },
  "platform_ids": {
    "description": "平台实例范围",
    "type": "list",
    "default": [],
    "hint": "留空表示全部实例；填写 AstrBot 平台实例 ID，不是飞书 App ID。"
  },
  "enable_group": {
    "description": "群聊开关",
    "type": "bool",
    "default": false
  },
  "group_all": {
    "description": "启用全部群聊",
    "type": "bool",
    "default": false,
    "hint": "关闭时仅启用下方指定列表。"
  },
  "group_ids": {
    "description": "指定群聊列表",
    "type": "list",
    "default": [],
    "hint": "填写群号（oc_ 开头）；空列表且未开启全部时，不启用任何会话。"
  },
  "excluded_group_ids": {
    "description": "排除群聊列表",
    "type": "list",
    "default": [],
    "hint": "优先于全部和指定列表；填写群号（oc_ 开头）。"
  },
  "enable_private": {
    "description": "私聊开关",
    "type": "bool",
    "default": false
  },
  "private_all": {
    "description": "启用全部私聊",
    "type": "bool",
    "default": false,
    "hint": "关闭时仅启用下方指定列表。"
  },
  "private_ids": {
    "description": "指定私聊列表",
    "type": "list",
    "default": [],
    "hint": "填写用户 Open ID（ou_ 开头）；空列表且未开启全部时，不启用任何会话。"
  },
  "excluded_private_ids": {
    "description": "排除私聊列表",
    "type": "list",
    "default": [],
    "hint": "优先于全部和指定列表；填写用户 Open ID（ou_ 开头）。"
  },
  "auto_topic_title": {
    "description": "自动总结话题标题",
    "type": "bool",
    "default": false,
    "hint": "话题已有问答后，后台调用当前会话配置的模型生成简短标题。每个话题成功生成一次，额外消耗少量模型额度；不压缩历史、不向聊天发送总结。"
  },
  "auto_topic_title_timeout": {
    "description": "标题模型等待时间（秒）",
    "type": "int",
    "default": 120,
    "hint": "单个模型的等待上限，范围20–240秒，默认120。推理模型可能需要较长时间；后台运行不阻塞聊天。整个标题任务最多300秒。"
  }
}
