# 升级说明

## 从 0.2.x 升级到 0.3.0

1. 确认 AstrBot 为 **4.28.0**，机器人使用飞书内置 Agent。其他版本或执行器暂不在兼容范围。
2. 等正在处理的请求结束，备份 AstrBot 数据、插件配置及 `data/plugin_data/astrbot_plugin_quote_topics/`。使用 SQLite 在线备份，或停止服务后复制整个数据目录，避免漏掉 WAL 文件。
3. 在插件管理中更新插件并重新加载，检查版本和加载日志。保留原有群聊、私聊、排除列表及自动标题设置。
4. 先用已登记的原始提问验证引用续聊，再验证机器人回复和自动标题。
5. 若使用卡片按钮或表单，确认交互插件也已按 [约定 v1](CONTINUATION_PROTOCOL.md) 接入。更新话题插件本身不会让未接入的按钮自动续聊。

插件首次加载时将索引从 schema 1 升级为 schema 2，保留既有话题和消息关联，增加交互去重表。聊天历史仍由 AstrBot 管理，不会复制或压缩。既有自动标题不会批量重命名。

Docker 部署应备份挂载在宿主机上的持久化数据，而不是只保留镜像或容器中的插件代码。

## 回退

0.2.x 不能读取 schema 2。需要回退时，停止相关请求和服务，恢复升级前的插件版本及配套数据备份。恢复备份会丢失备份之后的变更，请先另存当前数据。不要删除索引来规避版本检查，否则已登记消息将失去话题关联。

## 关闭或卸载

先等当前请求完成，再关闭总开关或卸载。未启用的会话使用 AstrBot 原来的聊天方式，已保存的对话和标题不会因关闭开关而消失。

平台禁用或卸载界面的数据删除选项由 AstrBot 管理；需要保留历史关联时，不要主动删除插件数据目录。插件不提供自动清理机制。
