# 开发与正式发布

## AstrBot 插件分支与发布规范

- 所有 AstrBot 插件仓库必须保留 `main`（正式分支）和 `develop`（开发分支）；开发分支统一叫 `develop`，不另设 `dev`。
- 每次完成一轮插件修改并通过必要检查后，自动在 `develop` 创建中文 Git 提交并推送该分支，随后通过既有运维部署入口将该已推送的固定提交部署到服务器 AstrBot，供用户测试；这是持续授权，无需逐次确认。保留其他未提交改动，只提交本轮相关文件。推送失败不部署，部署失败如实报告并按既有回滚流程处理，不强推、不绕过检查。
- 服务器测试使用既有 AstrBot 实例；部署前遵守已有备份、部署锁与回滚流程，部署后检查插件加载和服务健康并报告提交 SHA、结果。不得在本机启动第二条生产飞书连接，不自动发送测试消息。该流程由开发任务收尾执行，不新增 GitHub Actions 自动发布。
- 开发期间更新 CHANGELOG 的 Unreleased 部分；测试满意、准备候选版本时才更新 `metadata.yaml` 的版本号及正式更新说明，不为每次小改动升版。
- 经用户决定将候选代码合并到 `main` 后，由用户本人手动创建和推送带注释的测试标签 `vX.Y.Z-test.N`，标注验证范围、结果及已知限制。候选修复回到 `develop`，重新合并后由用户手动递增 N。
- 对 `main` 的候选提交完成测试验收后，由用户本人在同一提交手动创建和推送带注释的正式标签 `vX.Y.Z`；标签版本必须与 `metadata.yaml` 一致。所有标签（包括测试、正式及其他标签）均由用户手动操作，Agent 不创建、不推送、不移动、不删除任何标签。测试标签与正式标签不可移动或复用。
- 不配置 GitHub Actions 自动发布工作流。正式发布前人工核对提交属于 `main`、版本一致，且存在同版本、同提交的测试标签及验收记录。
- 正式标签仅记录用户已验收版本，不作为自动发布或送审触发器。自动提交、推送与服务器测试部署不包含合并 main、打标签或市场送审；推送使用明确的 develop 分支 refspec 并关闭 push.followTags，不使用 --tags、--follow-tags 或 --mirror。
- GitHub Release/安装包发布与 AstrBot Cloud 市场审核分开记录。未验证 Cloud 自动送审接口前，不伪造 API、不宣称 GitHub Release 等于市场上架；Cloud 手动入口为「＋发布 → 选择现有仓库 → 解析 → 更新插件版本」。GitHub App 的外部自动同步机制尚未确认，删除 Actions 不代表关闭该 App 的外部同步。

日常修改由 Agent 自动提交并推送 develop，再部署服务器测试。用户决定合并 main 后，测试标签和验收后的正式标签均由用户本人手动创建、推送。准备对外更新时，前往 https://cloud.astrbot.app/publish 选择仓库、解析并核对版本，最后手动点击「更新插件版本」送审。GitHub Release 如有需要可另行手动创建。

送审后在插件详情的「版本」页查看审核结果。回滚使用先前固定标签；不强制移动既有标签。

- 插件市场安装包自动排除开发文档：在 `.gitattributes` 使用 `export-ignore` 排除协作规范、开发/架构/验收/发布说明、插件间开发协议和测试资料；新增开发文档时同步补充规则。保留 README、CHANGELOG、LICENSE、用户升级/排障说明及运行时文件。README 引用被排除的文档时使用 GitHub 链接。发布前检查实际 ZIP 文件清单；此规则适用于 Git archive/GitHub 源码归档，不会隐藏 GitHub 仓库文件，也不会过滤 git clone 或任意压缩工具的结果。
